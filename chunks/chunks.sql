
-- SELECT moving parts

SELECT
FROM
WHERE
GROUP BY
ORDER BY
LIMIT

-- tabulate a column

SELECT   col, COUNT(*)
FROM
GROUP BY col
ORDER BY col DESC;

-- get a sample

WHERE random() < 0.05
LIMIT 30

