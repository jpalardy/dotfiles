#!/usr/bin/env awk -f

# URLs
/^\s*https?:/ {
  print $0 | "bookmark"
  next
}

# spanish accents
{
  gsub("a'", "á")
  gsub("i'", "í")
  gsub("u'", "ú")
  gsub("u:", "ü")
  gsub("e'", "é")
  gsub("o'", "ó")
  gsub("n~", "ñ")
}

# print everything else as-is
{
  print
}

