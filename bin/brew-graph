#!/bin/bash

remove_casks() {
  awk -F'"' 'NR == FNR {lut[$0] = 1; next} !($2 in lut) {print}' <(brew list --cask) -
}

brew-graph.rb --installed --highlight-leaves | remove_casks | neato -Tpng -o brew-deps.png
open brew-deps.png

