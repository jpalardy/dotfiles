#!/bin/bash

setup() {
  tmux send-keys "vim -O gtd/tasks.moe gtd/research.moe gtd/short.moe"
  tmux rename-window gtd

  tmux new-window
  tmux rename-window memora
  tmux send-keys "clear; cd supermemo; bake upcoming"

  tmux select-window -t 0
}

cd ~/Documents/wiki

(sleep 1; setup) &

tmux

