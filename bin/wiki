#!/bin/bash

setup() {
  tmux send-keys "vim -O gtd/tasks.moe gtd/research.moe"

  tmux new-window
  tmux send-keys "clear; cd supermemo; ls -l"

  tmux new-window
  tmux send-keys "clear; ls -l"

  tmux select-window -t 0
}

cd ~/Documents/wiki

(sleep 1; setup) &

tmux

