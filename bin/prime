#!/bin/bash

if [ -z "$1" ]; then
  echo >&2 "prime: no primer given"
  exit 1
fi

if [ ! -d "$HOME/dotfiles/bin/primers/$1" ]; then
  echo >&2 "prime: no primer for $1"
  exit 1
fi

rsync -av --backup "$HOME/dotfiles/bin/primers/$1/" .

